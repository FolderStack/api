authoriser:
  handler: ./src/service-authoriser/handlers/authorise.handler
  timeout: 30
  memorySize: 512
  environment:
      AUDIENCE: ${ssm:/damdam/${self:custom.currentStage}/AUDIENCE, null}
      JWKS_URI: ${ssm:/damdam/${self:custom.currentStage}/JWKS_URI, null}
      TOKEN_ISSUER: ${ssm:/damdam/${self:custom.currentStage}/TOKEN_ISSUER, null}