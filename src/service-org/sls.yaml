getOrgConfig:
  handler: ./src/service-org/handlers/getConfig.handler
  events:
    - http:
        method: GET
        path: /org/me
        cors: true
        caching:
          enabled: true
        authorizer:
          name: authoriser